# --- Confidential client data — NEVER commit (regenerate via the engine exporter) ---
# Real Allison vendor names + spend. Generated by navanta-lens-engine/jobs/export_fe_fixtures.py
src/data/fixtures/
# Raw spend cubes, discovery deck, screenshots in docs/ (keep the .md specs, drop the binaries)
docs/*.xlsb
docs/*.pptx
docs/*.pdf
docs/*.png

# --- Secrets ---
.env
.env*.local
# .npmrc auths the @navanta-ai GitHub Packages scope via ${GITHUB_TOKEN} (env, not literal)
.npmrc

# --- Next.js / Node ---
node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# --- misc ---
.DS_Store
npm-debug.log*
yarn-error.log*
