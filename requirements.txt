# Navanta Lens engine + copilot core — local validation stack.
# (On Databricks, pandas/pyarrow are provided; anthropic is needed for vendor verification + the live copilot.)
pandas>=2.0
pyarrow>=14.0
openpyxl>=3.1
PyYAML>=6.0
anthropic>=0.40
pytest>=8.0
