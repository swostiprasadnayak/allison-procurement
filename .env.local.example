# Copy this file to `.env.local` (which is gitignored) and put your real key in it.
# The classify-vendors hybrid run loads this file; the key is never printed or committed.
#
#   cp .env.local.example .env.local
#   # then edit .env.local and replace the placeholder
#
ANTHROPIC_API_KEY=sk-ant-REPLACE-ME
